# Legacy material

`original_snapshot/` contains the uploaded source files exactly as they existed before the refactor.

Use this folder for:

- audit comparison
- migration verification
- confirming that the refactor preserved the original experiment logic

Do not use it as the normal entry point for day-to-day runs; use the packaged `edgerag` modules instead.
